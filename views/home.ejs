<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home</title>
</head>
<body>
  <h1>
    <% let restaurantName = true %>
    <% if (restaurantName) {%>
    <%= restaurant.name %>
    <% } %>
</h1>
  <% let showMessage = true %>
  <% if (showMessage) {%>
  <!-- <p><%= restaurant.name %></p> -->
  <p><%= restaurant.address %></p>
  <p><%= restaurant.phone %></p>
  <% } %>
</body>
</html>